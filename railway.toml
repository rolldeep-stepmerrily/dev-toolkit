[build]
builder = "nixpacks"
buildCommand = "npm install -g pnpm && pnpm install --frozen-lockfile && pnpm --filter @repo/api build"

[build.nixpacksPlan.variables]
NIXPACKS_NODE_VERSION = "22.12.0"

[deploy]
startCommand = "node apps/api/dist/main.js"
restartPolicyType = "on_failure"
